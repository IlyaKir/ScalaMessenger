curl \
    -X POST \
    -H "Content-Type: application/json" \
    -H "user-name: user1" \
    --data '{
        "query": "query { allUsers { userId, name } }"
    }' \
    http://localhost:9000/graphql
